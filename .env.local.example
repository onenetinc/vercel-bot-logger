# Environment variables for local testing
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to git (it's already in .gitignore)

# Google Cloud Project Configuration
GCP_PROJECT=your-gcp-project-id
DATASET_ID=your_bigquery_dataset
TABLE_ID=your_bigquery_table

# Vercel Log Drain Secrets
# These should match the secrets configured in Vercel Dashboard
VERCEL_LOG_DRAIN_SECRET=your-vercel-log-drain-secret
VERCEL_VERIFY_TOKEN=your-vercel-verification-token

# Notes:
# - GCP_PROJECT: Your Google Cloud Project ID (e.g., "my-project-123")
# - DATASET_ID: BigQuery dataset name (e.g., "bot_logs")
# - TABLE_ID: BigQuery table name (e.g., "vercel_bot_traffic")
# - VERCEL_LOG_DRAIN_SECRET: Secret from Vercel log drain configuration
# - VERCEL_VERIFY_TOKEN: Token you choose for Vercel verification header
